import React from 'react';
import { ReactComponent as Pin } from './../../../assets/icons/location-pin.svg';

const locationPin = props => {
    return <Pin className={props.className} />
}

export default locationPin;
